import { PurchaseOrderResponse } from './purchase-order-response';

describe('PurchaseOrderResponse', () => {
  it('should create an instance', () => {
    expect(new PurchaseOrderResponse()).toBeTruthy();
  });
});
