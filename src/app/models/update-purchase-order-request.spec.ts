import { UpdatePurchaseOrderRequest } from './update-purchase-order-request';

describe('UpdatePurchaseOrderRequest', () => {
  it('should create an instance', () => {
    expect(new UpdatePurchaseOrderRequest()).toBeTruthy();
  });
});
