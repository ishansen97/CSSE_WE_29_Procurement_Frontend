import { ManagerBudgetClass } from './manager-budget-class';

describe('ManagerBudgetClass', () => {
  it('should create an instance', () => {
    expect(new ManagerBudgetClass()).toBeTruthy();
  });
});
